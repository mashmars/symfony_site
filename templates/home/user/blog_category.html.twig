{% extends 'home/common_user.html.twig' %}
{% block breadcrumb %}
          <div class="grid-x">
              <nav aria-label="You are here:" role="navigation">
                  <ul class="breadcrumbs">
                    <li><a href="#0">Home</a></li>
                    <li><a href="#0">Features</a></li>                    
                    <li>
                      <span class="show-for-sr">Current: </span> Cloning
                    </li>
                  </ul>
              </nav>  
          </div>
          {% endblock %}                  
            {% block right %}
            <div class="cell large-8 medium-7 margin-top-2 padding-1 shadow">                
               <a class="button small hollow" href="{{ path('user_blog_category_add') }}"><i class="fi-plus"></i> 新增</a>
               <hr>
               {% for type,messages in app.flashes %}
              <div class="cell">
                  <div class="{{ type }} callout" data-closable="slide-out-right">
                    {% for message in messages %}
                    <p>{{ message }}</p>
                    {% endfor %}
                    <button class="close-button" aria-label="Dismiss alert" type="button" data-close>
                      <span aria-hidden="true">&times;</span>
                    </button>
                  </div>
                </div>
                {% endfor %}
              <table class="hover">
                <thead>
                  <th>ID</th>
                  <th>名称</th>
                  <th>操作</th>
                </thead>
                <tbody>
                {% for cate in category %}
                  <tr>
                    <td>{{ cate.id }}</td>
                    <td>{{ cate.name }}</td>
                    <td>
                      <a class="button hollow tiny" href="{{ path('user_blog_category_edit',{ 'slug':cate.id })}}"><i class="fi-pencil"></i> 编辑</a>
                      <a class="button hollow tiny alert" href="{{ path('user_blog_category_delete',{'slug':cate.id}) }}"><i class="fi-trash"></i> 删除</a>
                    </td>
                  </tr>
                {% endfor %}
                </tbody>
              </table>
            </div>
            {% endblock %}
         